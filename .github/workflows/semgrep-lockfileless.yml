name: Semgrep SCA Pipeline Lockfileless

on:
  workflow_dispatch:
  pull_request:

jobs:
  semgrep-the-sca:
    runs-on: ubuntu-latest
    container: semgrep/semgrep:1.144.0
    steps:
      - uses: actions/checkout@v4
      - name: Run Semgrep SCA Lockfileless
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
        run: |
          ls -lah
          tree
          java -version
          sbt --version
          python3 --version
          semgrep ci --supply-chain --allow-local-builds --verbose --json --output=report-ssc.json || true
      - uses: actions/upload-artifact@v4
        with:
          name: semgrep-report
          path: report-ssc.json
